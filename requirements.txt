# ─────────────────────────────────────────────────────────────────────────────
# ATENÇÃO: moviepy 1.0.3 é incompatível com numpy >= 2.0 (usa np.bool_ etc.)
# Não atualize o numpy sem antes verificar compatibilidade com moviepy.
# ─────────────────────────────────────────────────────────────────────────────
numpy==1.22.0

# ── Vídeo / Imagem ────────────────────────────────────────────────────────────
moviepy==1.0.3
decorator==4.4.2
imageio==2.37.2
imageio-ffmpeg==0.4.9
proglog==0.1.10
Pillow==10.4.0
scipy==1.11.4

# ── Áudio ─────────────────────────────────────────────────────────────────────
pydub==0.25.1
mutagen==1.47.0
edge-tts==7.2.7
nest-asyncio==1.6.0

# ── Google: YouTube upload + Google Cloud TTS ────────────────────────────────
google-api-python-client==2.184.0
google-auth==2.41.1
google-auth-oauthlib==1.2.2
google-cloud-texttospeech==2.33.0

# ── AWS Polly ────────────────────────────────────────────────────────────────
boto3==1.34.162

# ── Whisper — transcrição/alinhamento de legendas ────────────────────────────
# torch é instalado separadamente pelo setup.sh (varia por CUDA disponível)
openai-whisper==20250625

# ── IA / Geração (Gemini + Pollinations) ─────────────────────────────────────
google-generativeai==0.8.5

# ── Remoção de fundo (rembg) ──────────────────────────────────────────────────
rembg==2.0.67
opencv-python-headless==4.12.0.88
onnxruntime==1.23.2

# ── Utilitários ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1
srt==3.5.3
thefuzz==0.22.1
requests==2.32.5
aiohttp==3.12.15
psutil==6.0.0
tqdm==4.66.1

# ── Corte e transcrição de pregações (reelscutter / a.py) ────────────────────
yt-dlp==2026.3.17
youtube-transcript-api==1.2.4
